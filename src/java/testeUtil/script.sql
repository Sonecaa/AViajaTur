CREATE TABLE Passagem (IDPASSAGEM BIGINT NOT NULL, DATAEHORA DATETIME, VALOR DOUBLE, idOnibus BIGINT, PRIMARY KEY (IDPASSAGEM))
CREATE TABLE LINHA (IDLINHA BIGINT NOT NULL, NOME VARCHAR(255), PRIMARY KEY (IDLINHA))
CREATE TABLE CLIENTE (IDCLIENTE BIGINT NOT NULL, NOME VARCHAR(255), PERMISSAO TINYINT(1) default 0, SENHA VARCHAR(255), PRIMARY KEY (IDCLIENTE))
CREATE TABLE ONIBUS (IDONIBUS BIGINT NOT NULL, NUMPOLTRONAS INTEGER, idLinha BIGINT, PRIMARY KEY (IDONIBUS))
CREATE TABLE VENDA (IDVENDA BIGINT NOT NULL, DATAEHORAVENDA DATETIME, idCliente BIGINT, idPassagem BIGINT, PRIMARY KEY (IDVENDA))
ALTER TABLE Passagem ADD CONSTRAINT FK_Passagem_idOnibus FOREIGN KEY (idOnibus) REFERENCES ONIBUS (IDONIBUS)
ALTER TABLE ONIBUS ADD CONSTRAINT FK_ONIBUS_idLinha FOREIGN KEY (idLinha) REFERENCES LINHA (IDLINHA)
ALTER TABLE VENDA ADD CONSTRAINT FK_VENDA_idPassagem FOREIGN KEY (idPassagem) REFERENCES Passagem (IDPASSAGEM)
ALTER TABLE VENDA ADD CONSTRAINT FK_VENDA_idCliente FOREIGN KEY (idCliente) REFERENCES CLIENTE (IDCLIENTE)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
